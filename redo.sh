#!/usr/bin/env bash

redo() {
  echo ${BRANCH}
  # git checkout `pbpaste`

  # git checkout master
  # git fetch origin
  # git pull
  # git checkout `pbpaste`
  # git rebase master
  # git push origin `pbpaste` -f
  # git checkout master
  # git merge --squash `pbpaste`
}

redo
